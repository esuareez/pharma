@model IEnumerable<Pharma.Models.OrdenCompra>
@{
    ViewData["Title"] = "Products";
    Layout = "~/Views/Shared/layoutAdmin.cshtml";
}

<div id="main-content container-fluid">
                <div class="page-heading">
                    <div class="page-title">
                        <div class="row">
                            <div class="col-12 col-md-6 order-md-1 order-last">
                                <h3>Ordenes de compra</h3>
                                <p class="text-subtitle text-muted">Ver, modificar, eliminar las ordenes de compra.</p>
                                 <a asp-controller="Orden" asp-action="Create" class="btn btn-success">Crear orden</a>
                                 <br>
                            </div>
                            <div class="col-12 col-md-6 order-md-2 order-first">
                                <nav aria-label="breadcrumb" class="breadcrumb-header float-start float-lg-end">
                                    <ol class="breadcrumb">
                                        <li class="breadcrumb-item"><a href="index.html">Dashboard</a></li>
                                        <li class="breadcrumb-item active" aria-current="page">Products</li>
                                    </ol>
                                </nav>
                            </div>
                        </div>
                    </div>
                @if (Model.Count() > 0)
                {
                <section class="section">
                    <div class="card">
                        <div class="card-body">
                            <table class="table table-striped" id="table1">
                                <thead>
                                    <tr>
                                        <th>Proveedor</th>
                                        <th>Fecha</th>
                                        <th>Monto a pagar</th>
                                        <th>Acción</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                                <td>@item.IdProveedorNavigation.Nombre</td>
                                                <td>@item.Fecha</td>
                                                <td>@item.MontoPagar</td>
                                                <td>
                                                    <a class="btn btn-success" asp-controller="Producto" asp-action="Edit" asp-route-id=@item.IdOrComp><i class="fa-solid fa-pen-to-square"></i></a>
                                                    @*<a class="btn btn-danger"><i class="fa-solid fa-trash-can"></i></a>*@
                                                </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                </section>
        
           }else{
                <br><p>No existen registros de una orden de compra.</p>
            }
            </div>
</div>
        
    
        <script src="~/css/css-emp/vendors/simple-datatables/simple-datatables.js"></script>
        <script>
            // Simple Datatable
            let table1 = document.querySelector('#table1');
            let dataTable = new simpleDatatables.DataTable(table1);
        </script>

